overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/graphql/**/*.graphql"
generates:
    src/gql:
        preset: "client"
        plugins: []
    ./graphql.schema.json:
        plugins:
            - "introspection"
