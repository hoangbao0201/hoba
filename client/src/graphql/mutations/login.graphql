mutation Login($loginInput: LoginInput!) {
    login(loginInput: $loginInput) {
        code
        success
        message
        user {
            username
            email
            password
            createAt
            updateAt
        }
        accessToken
        errors {
            field
            message
        }
    }
}
